import * as Colyseus from "colyseus.js";

export default Colyseus;
// const client = new Colyseus.Client("ws://localhost:3000");
// const room = client
//   .joinOrCreate("tictactoe")
//   .then((room) => console.log("client sucess joined: ", room.sessionId));
// room
